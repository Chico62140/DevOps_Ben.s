apiVersion: v1
kind: Service
metadata:
  name: flask-ben-service
spec:
  selector:
    app: flask-ben
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30000
  type: NodePort
